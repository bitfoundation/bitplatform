@page "/reset-password"

@inherits TodoTemplateComponentBase;

<PageTitle>Reset password</PageTitle>

<div class="page-container">
    <EditForm Model="ResetPasswordModel" OnValidSubmit="WrapHandled(Submit)" class="form--white">

        <DataAnnotationsValidator />

        @if (string.IsNullOrEmpty(ResetPasswordMessage) is false)
        {
            <BitMessageBar Class="form-message-bar"
                       MessageBarType="@ResetPasswordMessageType"
                       OnDismiss="() => ResetPasswordMessage = null">
                @ResetPasswordMessage
            </BitMessageBar>
        }
        <h1 class="form-title">Reset password</h1>
        <div class="form-input-container">
            <BitLabel>New Password</BitLabel>
            <BitTextField @bind-Value="ResetPasswordModel.NewPassword"
                          Type="BitTextFieldType.Password"
                          CanRevealPassword="true"
                          Placeholder="New Password" />
            <ValidationMessage For="@(() => ResetPasswordModel.NewPassword)" />
        </div>
        <div class="form-input-container">
            <BitLabel>Confirm New Password</BitLabel>
            <BitTextField @bind-Value="ResetPasswordModel.ConfirmNewPassword"
                          Type="BitTextFieldType.Password"
                          CanRevealPassword="true"
                          Placeholder="Confirm New Password" />
            <ValidationMessage For="@(() => ResetPasswordModel.ConfirmNewPassword)" />
        </div>
        <BitButton Class="form-submit-button"
                   ButtonStyle="BitButtonStyle.Primary"
                   Title="Submit"
                   AriaLabel="Submit"
                   IsEnabled="@context.Validate()"
                   ButtonType="BitButtonType.Submit">
            @if (IsLoading)
            {
                <BitSpinner Size="BitSpinnerSize.Small"></BitSpinner>
            }
            else
            {
                <span>Change my password</span>
            }
        </BitButton>
    </EditForm>
</div>