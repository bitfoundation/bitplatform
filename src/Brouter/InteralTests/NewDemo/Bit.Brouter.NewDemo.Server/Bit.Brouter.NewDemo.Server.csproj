<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <!-- Reuse the shared routes, pages and layout from the existing demo Core project so the
         prerendering showcase exercises the exact same Brouter setup the other heads use. -->
    <ProjectReference Include="..\..\Demo\Bit.Brouter.Demo.Core\Bit.Brouter.Demo.Core.csproj" />
  </ItemGroup>

</Project>
