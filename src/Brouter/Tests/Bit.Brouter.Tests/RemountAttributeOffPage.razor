@page "/rmattroff/{id:int}"
@attribute [BrouterRemountOnParameterChange(false)]

@* Attribute-discovered page that opts out of RemountOnParameterChange on itself. *@

<StatefulCounter />

@code {
    [Parameter] public int Id { get; set; }
}
