@page "/components/fileuploads"

@using Bit.Client.Web.BlazorUI.Playground.Web.Components

<ComponentPage ComponentName="FileUpload" OverviewDesc="BitFileUpload component wraps the HTML file input element(s) and uploads them to a given URL. The files can be removed by
    specifying the URL they have been uploaded. Moreover, it provides several other options including single or multiple or automatic file uploading.
    By automatic, it means the files can be automatically uploaded after being selected. It is possible to specify a maximum size for each file if need be.
    Additionally, by specifying file extensions, the files can be restricted to certain types."
               ComponentParameters="componentParameters" EnumParameters="enumParameters">
    <CodeSample Title="BitFileUpload" Description="Files can be uploaded automatically after selecting them.">
        <SampleContent>
            <div>
                <BitFileUpload Label="Select or drag and drop files"
                               UploadUrl="@UploadUrl"
                               RemoveUrl="@RemoveUrl">
                </BitFileUpload>
            </div>
        </SampleContent>
        <CodeSampleContent>
            <pre>
                &lt<span class="dark-green">BitFileUpload Label</span> ="Select or drag and drop files" 
                                               <span class="dark-green">UploadUrl</span> ="<span class="light-yellow-bg">&#64</span>UploadUrl" 
                                               <span class="dark-green">RemoveUrl</span> ="<span class="light-yellow-bg">&#64</span>RemoveUrl"&gt
                &lt/<span class="dark-green">BitFileUpload</span>&gt
                <span class="light-yellow-bg">&#64code {</span>
                  <span class="light-blue">private string</span> UploadUrl;
                  <span class="light-blue">private string</span> RemoveUrl;
                <span class="light-yellow-bg">}</span>
            </pre>
        </CodeSampleContent>
    </CodeSample>
    <CodeSample Title="Auto & Multiple" Description="Multiple files can be selected to upload automatically.">
        <SampleContent>
            <div>
                <BitFileUpload IsMultiSelect="true"
                               Label="Select or drag and drop files"
                               UploadUrl="@UploadUrl"
                               RemoveUrl="@RemoveUrl">
                </BitFileUpload>
            </div>
        </SampleContent>
        <CodeSampleContent>
            <pre>
                 &lt<span class="dark-green">BitFileUpload IsMultiSelect</span> =" <span class="light-blue">true</span>" 
                                                <span class="dark-green">Label</span>="Select or drag and drop files" 
                                                <span class="dark-green">UploadUrl</span>="<span class="light-yellow-bg">&#64</span>UploadUrl" 
                                                <span class="dark-green">RemoveUrl</span>="<span class="light-yellow-bg">&#64</span>RemoveUrl"&gt
                 &lt/<span class="dark-green">BitFileUpload</span>&gt
                <span class="light-yellow-bg">&#64code {</span>
                      <span class="light-blue">private string</span> UploadUrl;
                      <span class="light-blue">private string</span> RemoveUrl;
                <span class="light-yellow-bg">}</span>
            </pre>
        </CodeSampleContent>
    </CodeSample>
    <CodeSample Title="Auto & Multiple with max-size limitation" Description="Multiple files can be selected to upload automatically with limited size.">
        <SampleContent>
            <div>
                <BitFileUpload IsMultiSelect="true"
                               Label="Select or drag and drop files"
                               MaxSize="1024 * 1024 * 100"
                               UploadUrl="@UploadUrl"
                               RemoveUrl="@RemoveUrl">
                </BitFileUpload>
            </div>
        </SampleContent>
        <CodeSampleContent>
            <pre>
                 &lt<span class="dark-green">BitFileUpload IsMultiSelect</span>="<span class="light-blue">true</span>" 
                                                <span class="dark-green">Label</span>="Select or drag and drop files" 
                                                <span class="dark-green">MaxSize</span>="1024 * 1024 * 100" 
                                                <span class="dark-green">UploadUrl</span>="<span class="light-yellow-bg">&#64</span>UploadUrl" 
                                                <span class="dark-green">RemoveUrl</span>="<span class="light-yellow-bg">&#64</span>RemoveUrl"&gt
                 &lt/<span class="dark-green">BitFileUpload</span>&gt
                 <span class="light-yellow-bg">&#64code {</span>
                       <span class="light-blue">private string</span> UploadUrl;
                       <span class="light-blue">private string</span> RemoveUrl;
                 <span class="light-yellow-bg">}</span>
            </pre>
        </CodeSampleContent>
    </CodeSample>
    <CodeSample Title="Individual & Multiple (limited by extensions format)" Description="Single or multiple file uploading limited only by file extensions, which in this case the allowed file extensions include gif, jpg and mp4.">
        <SampleContent>
            <div>
                <BitFileUpload IsMultiSelect="true"
                               AutoUploadEnabled="false"
                               AllowedExtensions="@(new List<string> { ".gif",".jpg",".mp4" })"
                               Label="Select or drag and drop files"
                               UploadUrl="@UploadUrl"
                               RemoveUrl="@RemoveUrl">
                </BitFileUpload>
            </div>
        </SampleContent>
        <CodeSampleContent>
            <pre>
                 &lt<span class="dark-green">BitFileUpload IsMultiSelect</span>="<span class="light-blue">true</span>" 
                                                <span class="dark-green">AutoUploadEnabled</span>="<span class="light-blue">false</span>" 
                                                <span class="dark-green">AllowedExtensions</span>="<span class="light-yellow-bg">&#64</span>(<span class="light-blue">new</span> <span class="dark-cyan">List</span>&lt <span class="light-blue">string</span>&gt { <span class="dark-orange">".gif"</span>,<span class="dark-orange">".jpg"</span>,<span class="dark-orange">".mp4"</span> })" 
                                                <span class="dark-green">Label</span>="Select or drag and drop files" 
                                                <span class="dark-green">UploadUrl</span>="<span class="light-yellow-bg">&#64</span>UploadUrl" 
                                                <span class="dark-green">RemoveUrl</span>="<span class="light-yellow-bg">&#64</span>RemoveUrl"&gt
                 &lt/<span class="dark-green">BitFileUpload</span>&gt
                <span class="light-yellow-bg">&#64code {</span>
                      <span class="light-blue">private string</span> UploadUrl;
                      <span class="light-blue">private string</span> RemoveUrl;
                <span class="light-yellow-bg">}</span>
            </pre>
        </CodeSampleContent>
    </CodeSample>
</ComponentPage>