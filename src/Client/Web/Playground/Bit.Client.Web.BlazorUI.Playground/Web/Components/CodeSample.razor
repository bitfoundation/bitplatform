<div class="sample-container">
    <h5 class="component-subtitle">@Title</h5>
    <p class="how-to-use">
        @Description
    </p>
    <BitToggleButton @bind-IsChecked="showCode"
                     Label="@(showCode ? "</> Hide code":"</> Show code")"
                     Class="@(showCode ? "hide-code-btn":"show-code-btn")">
    </BitToggleButton>

    <div class="box">
        @if (!showCode)
        {
            <div class="sample">
                @SampleContent
            </div>
        }
        else
        {
            <div class="code">
                <BitActionButton Class="copy-btn" Style="@(isCopied ? "border:none;" : string.Empty)"
                             OnClick=CopyCodeToClipboard
                             IconName="BitIcon.Copy">
                    @(isCopied ? "copied" : "copy")
                </BitActionButton>
                <div class="razor-code">
                    @CodeSampleContent
                </div>
            </div>
        }
    </div>
</div>