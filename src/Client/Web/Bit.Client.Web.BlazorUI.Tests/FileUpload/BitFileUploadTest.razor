<CascadingValue Value="Visual">
    <BitFileUpload IsEnabled="IsEnabled"
                   IsMultiFile="IsMultiFile"
                   BrowseButtonLabel="Select or drag and drop files"
                   UploadUrl="@UploadUrl"
                   RemoveUrl="@RemoveUrl">
    </BitFileUpload>
</CascadingValue>
@code {
    private readonly Uri UploadUrl = new("https://localhost:5001/FileUpload/UploadStreamedFile");
    private readonly Uri RemoveUrl = new("https://localhost:5001/FileUpload/RemoveFile");
    [Parameter] public Visual Visual { get; set; }
    [Parameter] public bool IsEnabled { get; set; }
    [Parameter] public bool IsMultiFile { get; set; }
}