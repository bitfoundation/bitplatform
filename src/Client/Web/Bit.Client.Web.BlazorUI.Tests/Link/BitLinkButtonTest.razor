<BitLink Text="Without an href, the link rendered as a button" OnClick="IncrementCount"></BitLink>

@code {
    public int CurrentCount { get; set; }

    public void IncrementCount(MouseEventArgs e)
    {
        CurrentCount++;
    }
}