<BitSpinButton Min="@Min" Max="@Max" Suffix="@Suffix"></BitSpinButton>

@code{
    [Parameter] public double Min { get; set; }
    [Parameter] public double Max { get; set; }
    [Parameter] public string Suffix { get; set; }
}