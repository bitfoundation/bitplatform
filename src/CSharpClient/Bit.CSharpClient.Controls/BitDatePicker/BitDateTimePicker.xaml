<?xml version="1.0" encoding="utf-8" ?>
<TemplatedView
    x:Class="Bit.View.Controls.BitDateTimePicker"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:bitControls="clr-namespace:Bit.View.Controls">
    <TemplatedView.Resources>
        <Style TargetType="bitControls:BitDateTimePicker">
            <Setter Property="ControlTemplate">
                <ControlTemplate>
                    <Button
                        Command="{TemplateBinding OpenPopupCommand}"
                        FontFamily="{TemplateBinding FontFamily}"
                        Text="{TemplateBinding DisplayText}" />
                </ControlTemplate>
            </Setter>
        </Style>
    </TemplatedView.Resources>
</TemplatedView>