@inherits LayoutComponentBase

<Header />

<div class="main @(CurrentUrl == "/" ? "main--full-page" : null)">
    @if (CurrentUrl != "/")
    {
        <NavMenu />
    }
    <div class="site-content">
        @Body
    </div>
</div>

<Footer />