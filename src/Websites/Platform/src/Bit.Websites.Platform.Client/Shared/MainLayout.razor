@inherits LayoutComponentBase

<Header />

<div class="main @(navMenuService.IsDocsRoute ? null : "main--full-page")">
    @if (navMenuService.IsDocsRoute)
    {
        <NavMenu NavItems="navItems" />
    }

    <div class="site-content @(navMenuService.IsDocsRoute ? "docs-content" : "")" id="main-content">
        <AppErrorBoundary>
            @Body
        </AppErrorBoundary>
    </div>
</div>

<Footer />

<MessageBox />
<AppAiChatPanel />
