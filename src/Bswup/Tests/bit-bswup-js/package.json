{
  "name": "bit-bswup-js-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Unit tests for the bit-bswup browser bundles (service worker, page script, progress UI)",
  "scripts": {
    "test": "vitest run",
    "watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
