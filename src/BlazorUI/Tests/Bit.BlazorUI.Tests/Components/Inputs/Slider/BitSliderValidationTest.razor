@using Bit.BlazorUI.Tests.Components.Inputs.Slider

<EditForm Model="@TestModel" OnValidSubmit="@HandleValidSubmit" OnInvalidSubmit="@HandleInvalidSubmit">
    <DataAnnotationsValidator />

    <BitSlider Required Name="days" Max="7" @bind-Value="@TestModel.Days" />

</EditForm>

@code {
    [Parameter] public BitSliderTestModel TestModel { get; set; } = new();

    public int ValidCount { get; set; }
    public int InvalidCount { get; set; }

    private void HandleValidSubmit()
    {
        ValidCount++;
    }

    private void HandleInvalidSubmit()
    {
        InvalidCount++;
    }
}
