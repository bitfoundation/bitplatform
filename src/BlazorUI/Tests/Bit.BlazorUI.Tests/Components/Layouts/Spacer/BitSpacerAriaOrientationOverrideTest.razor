@* The orientation a labelled spacer takes is rendered before the splatted attributes, so a flexible spacer
   dividing the items of a column can still state it itself. *@

<BitSpacer AriaLabel="spacer" aria-orientation="horizontal" />
