@* Covers an aria-label written in the markup (which binds no parameter) while AriaLabel is left unset: it has to survive. *@

<BitGrid aria-label="Products">
    <BitGridItem aria-label="Summary">Item</BitGridItem>
</BitGrid>
