@page "/components/nav"
@inherits AppComponentBase

<PageOutlet Url="components/nav"
            Title="Nav"
            Description="A navigation pane (Nav) provides links to the main areas of an app or site, and can also be used as a TreeView to show parent-child data in a tree." />

<div>
    <DemoPage Name="Nav"
              SecondaryNames="@(["Tree", "TreeView"])"
              Description="A navigation pane (Nav) provides links to the main areas of an app or site, and can also be used as a TreeView to show parent-child data in a tree. It renders a hierarchy of any depth, keeps its selection in sync with the current URL (exact, prefix, wildcard or regex) or leaves it to the app, groups items under headers, shrinks to a rail of icons, and is fully operable from the keyboard with the tree-view keys."
              Parameters="componentParameters"
              CssVariables="componentCssVariables"
              PublicMembers="componentPublicMembers"
              SubClasses="componentSubClasses"
              SubEnums="componentSubEnums"
              GitHubUrl="Navs/Nav/BitNav.razor"
              GitHubDemoUrl="Navs/Nav/BitNavDemo.razor">
        <NotesTemplate>
            <BitText>
                The BitNav is a <BitTag Color="BitColor.SecondaryBackground">Multi-API</BitTag> component
                which can accept the list of Items in 3 different ways:
                <br />
                1. The <BitLink Href="#nav-item"><BitTag Color="BitColor.SecondaryBackground">BitNavItem</BitTag></BitLink> class <br />
                2. A <BitTag Color="BitColor.SecondaryBackground">Custom Generic</BitTag> class <br />
                3. The <BitLink Href="#nav-option"><BitTag Color="BitColor.SecondaryBackground">BitNavOption</BitTag></BitLink> component
            </BitText>
        </NotesTemplate>
        <Examples>
            <BitPivot Classes="@(new() { HeaderContainer = "pivot-sticky-header" })" MountAll="RenderForMcpClient">
                <BitPivotItem HeaderText="Item">
                    <_BitNavItemDemo />
                </BitPivotItem>

                <BitPivotItem HeaderText="Custom">
                    <_BitNavCustomDemo />
                </BitPivotItem>

                <BitPivotItem HeaderText="Option">
                    <_BitNavOptionDemo />
                </BitPivotItem>
            </BitPivot>
        </Examples>
    </DemoPage>
</div>
