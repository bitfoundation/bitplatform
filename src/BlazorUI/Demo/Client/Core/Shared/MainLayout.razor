@namespace Bit.BlazorUI.Demo.Client.Core
@inherits LayoutComponentBase

<div class="status-bar">@_pageTitle - Bit BlazorUI</div>

<Header />

<main class="@(CurrentUrl == "/" ? "full-page" : "")  @BitCss.Class.Color.Background.Primary">
    @if (CurrentUrl != "/")
    {
        <NavMenu />
    }
    <div class="site-content">
        <AppErrorBoundary>
            @Body
        </AppErrorBoundary>
    </div>
</main>

<MessageBox />
<Footer />