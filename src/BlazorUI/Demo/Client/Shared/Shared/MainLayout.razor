@namespace Bit.BlazorUI.Demo.Client.Shared
@inherits LayoutComponentBase

<div class="status-bar">Bit BlazorUI</div>

<Header />

<main class="@(CurrentUrl == "/" ? "full-page" : "") @BitCss.Background.Primary">
    @if (CurrentUrl != "/")
    {
        <NavMenu />
    }
    <div class="site-content">
        <AppErrorBoundary>
            @Body
        </AppErrorBoundary>
    </div>
</main>

<MessageBox />
<Footer />