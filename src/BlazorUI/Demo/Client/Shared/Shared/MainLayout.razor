@namespace Bit.BlazorUI.Demo.Client.Shared
@inherits LayoutComponentBase

<Header />

<main class="bit-bg-primary @(CurrentUrl == "/" || CurrentUrl == "/icons" ? "full-page" : "")">
    @if (CurrentUrl != "/")
    {
        <NavMenu />
    }
    <div class="site-content">
        <AppErrorBoundary>
            @Body
        </AppErrorBoundary>
    </div>
</main>

<MessageBox />
<Footer />