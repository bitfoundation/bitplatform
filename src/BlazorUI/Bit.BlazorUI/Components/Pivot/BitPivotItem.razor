@namespace Bit.BlazorUI
@inherits BitComponentBase

<button @ref="RootElement"
        @attributes="HtmlAttributes"
        style="@StyleBuilder.Value"
        class="@ClassBuilder.Value"
        type="button"
        role="tab"
        id="@Pivot?.GetPivotItemId(this)"
        aria-selected="@(IsSelected ? "true": "false")"
        aria-label="@AriaLabel"
        name="@HeaderText"
        data-content="@HeaderText"
        tabindex="@Pivot?.GetPivotItemTabIndex(this)"
        @onclick="HandleButtonClick">
    <span>
        @if (@IconName.HasValue)
        {
            <span>
                <i class="bit-icon bit-icon--@(IconName.Value.GetName())"></i>
            </span>
        }

        @if (HeaderFragment is not null)
        {
            @HeaderFragment
        }
        else if (HeaderText.HasValue())
        {
            <span>@HeaderText</span>
        }

        @if (ItemCount is not null)
        {
            <span>(@ItemCount)</span>
        }
    </span>
</button>
