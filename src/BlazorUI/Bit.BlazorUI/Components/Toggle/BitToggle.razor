@namespace Bit.BlazorUI
@inherits BitInputBase<bool>

<div @ref="RootElement"
     @attributes="HtmlAttributes"
     style="@StyleBuilder.Value"
     class="@ClassBuilder.Value">

    @if (LabelTemplate is not null)
    {
        <label for="@_buttonId" class="tgl-lbl">
            @LabelTemplate
        </label>
    }
    else if (Label.HasValue())
    {
        <label for="@_buttonId" class="tgl-lbl">
            @Label
        </label>
    }

    <div class="tgl-container">
        <button id="@_buttonId"
                class="tgl-btn"
                aria-label="@AriaLabel"
                aria-checked="@_ariaChecked"
                aria-labelledby="@_labelledById"
                role="@Role"
                type="button"
                data-is-focusable="true"
                data-ktp-target="true"
                @onclick="HandleOnClick">
            <span class="tgl-state"></span>
        </button>
        @if (_stateText.HasValue())
        {
            <label for="@_buttonId" class="state-txt">
                @_stateText
            </label>
        }
    </div>
    <input @attributes="InputHtmlAttributes" type="checkbox" value="@CurrentValueAsString" class="bit-input-hidden" />
</div>
