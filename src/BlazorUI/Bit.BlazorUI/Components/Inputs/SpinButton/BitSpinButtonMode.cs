namespace Bit.BlazorUI;

public enum BitSpinButtonMode
{
    /// <summary>
    /// Spinning buttons render as a stack at the end of the input.
    /// </summary>
    Stack,

    /// <summary>
    /// Spinning buttons render at the start and end of the input.
    /// </summary>
    Spread
}
