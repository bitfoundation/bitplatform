@page "/gestures"

<PageTitle>Gestures - Bit.Bmotion</PageTitle>

<div class="demo-section">
    <h2>Hover &amp; Tap</h2>
    <p>
        <strong>WhileHover</strong> and <strong>WhileTap</strong> overlay animation states
        on top of the base <c>Animate</c> state. They automatically revert on pointer-up / leave.
    </p>
    <div class="demo-row">
        <div class="demo-card">
            <Bmotion Animate="Bm.To(scale: 1)"
                    WhileHover='Bm.To(scale: 1.15, y: -6, boxShadow: "0 20px 40px rgba(108,71,255,.5)")'
                    WhileTap="Bm.To(scale: 0.92)"
                    Transition="Bm.Spring(stiffness: 400, damping: 25)">
                <div class="box" />
            </Bmotion>
        </div>

        <div class="demo-card">
            <Bmotion Animate="Bm.To(scale: 1)"
                    WhileHover='Bm.To(scale: 1.07, backgroundColor: "#8a66ff")'
                    WhileTap="Bm.To(scale: 0.95)"
                    Transition="Bm.Tween(0.15)">
                <button class="btn-bm">
                    Animated Button
                </button>
            </Bmotion>
        </div>
    </div>

    <CodeSnippet Code="@_hoverTapCode" />
</div>

<div class="demo-section">
    <h2>Focus</h2>
    <p><strong>WhileFocus</strong> plays while an element or its descendants are focused.</p>
    <div class="demo-row">
        <div class="demo-card">
            <Bmotion Animate="Bm.To(scale: 1)"
                    WhileFocus='Bm.To(scale: 1.03, borderColor: "#6c47ff")'
                    Transition="Bm.Tween(0.2)">
                <input style="background:var(--bm-card);border:2px solid var(--bm-border);color:var(--bm-text);border-radius:8px;padding:.5rem 1rem;outline:none;width:240px"
                       placeholder="Click to focus…"
                       type="text" />
            </Bmotion>
        </div>
    </div>

    <CodeSnippet Code="@_focusCode" />
</div>

<div class="demo-section">
    <h2>Events</h2>
    <p>Listen to gesture callbacks: <c>OnHoverStart</c>, <c>OnTap</c>, <c>OnTapCancel</c>, etc.</p>
    <div class="demo-row">
        <div class="demo-card" style="flex-direction:column;gap:1rem;">
            <Bmotion WhileHover="Bm.To(scale: 1.1)"
                    WhileTap="Bm.To(scale: 0.9)"
                    OnHoverStart="@(() => AddEvent("HoverStart"))"
                    OnHoverEnd="@(() => AddEvent("HoverEnd"))"
                    OnTapStart="@(() => AddEvent("TapStart"))"
                    OnTap="@(() => AddEvent("Tap"))"
                    OnTapCancel="@(() => AddEvent("TapCancel"))">
                <div class="box" />
            </Bmotion>
            <div style="font-size:.8rem;color:var(--bm-muted);max-height:120px;overflow:auto;">
                @foreach (var e in _events.TakeLast(8).Reverse())
                {
                    <div>@e</div>
                }
            </div>
        </div>
    </div>

    <CodeSnippet Code="@_eventsCode" />
</div>

@code {
    private readonly List<string> _events = new();

    void AddEvent(string name)
    {
        _events.Add($"[{DateTime.Now:HH:mm:ss.ff}] {name}");
        // Only the last 8 entries are ever shown; trim the rest so a long session can't grow this list unbounded.
        if (_events.Count > 8)
            _events.RemoveRange(0, _events.Count - 8);
        StateHasChanged();
    }

    private const string _hoverTapCode = """
        @using Bit.Bmotion

        <style>
            :root { --bm-primary: #1276C6; --bm-primary-text: #fff; --bm-secondary: #FD7F36; --bm-text: #1A1A1A; --bm-muted: #525252; --bm-card: #F5F5F5; --bm-track: #EBEBEB; --bm-border: #E0E0E0; --bm-radius: 2px; --bm-accent-soft: rgba(18,118,198,.12); --bm-accent-line: rgba(18,118,198,.35); }
            .demo-section { margin-bottom: 3rem; }
            .demo-section h2 { font-size: 1.4rem; font-weight: 700; margin-bottom: .5rem; }
            .demo-row { display: flex; flex-wrap: wrap; gap: 1.5rem; align-items: center; }
            .demo-card {
                background: var(--bm-card); border: 1px solid var(--bm-border);
                border-radius: 12px; padding: 2rem; min-height: 160px; flex: 1; min-width: 220px;
                display: flex; align-items: center; justify-content: center;
            }
            .box {
                width: 80px; height: 80px; border-radius: 10px;
                background: linear-gradient(135deg, var(--bm-primary), var(--bm-secondary));
            }
            .btn-bm {
                background: var(--bm-primary); color: var(--bm-primary-text); border: none; border-radius: 8px;
                padding: .6rem 1.4rem; font-weight: 600; cursor: pointer;
            }
        </style>

        <div class="demo-section">
            <h2>Hover &amp; Tap</h2>
            <div class="demo-row">
                <div class="demo-card">
                    <Bmotion Animate="Bm.To(scale: 1)"
                            WhileHover='Bm.To(scale: 1.15, y: -6, boxShadow: "0 20px 40px rgba(108,71,255,.5)")'
                            WhileTap="Bm.To(scale: 0.92)"
                            Transition="Bm.Spring(stiffness: 400, damping: 25)">
                        <div class="box" />
                    </Bmotion>
                </div>

                <div class="demo-card">
                    <Bmotion Animate="Bm.To(scale: 1)"
                            WhileHover='Bm.To(scale: 1.07, backgroundColor: "#8a66ff")'
                            WhileTap="Bm.To(scale: 0.95)"
                            Transition="Bm.Tween(0.15)">
                        <button class="btn-bm">
                            Animated Button
                        </button>
                    </Bmotion>
                </div>
            </div>
        </div>
        """;

    private const string _focusCode = """
        @using Bit.Bmotion

        <style>
            :root { --bm-primary: #1276C6; --bm-primary-text: #fff; --bm-secondary: #FD7F36; --bm-text: #1A1A1A; --bm-muted: #525252; --bm-card: #F5F5F5; --bm-track: #EBEBEB; --bm-border: #E0E0E0; --bm-radius: 2px; --bm-accent-soft: rgba(18,118,198,.12); --bm-accent-line: rgba(18,118,198,.35); }
            .demo-section { margin-bottom: 3rem; }
            .demo-section h2 { font-size: 1.4rem; font-weight: 700; margin-bottom: .5rem; }
            .demo-section p { color: var(--bm-muted); margin-bottom: 1.5rem; line-height: 1.6; }
            .demo-row { display: flex; flex-wrap: wrap; gap: 1.5rem; align-items: center; }
            .demo-card {
                background: var(--bm-card); border: 1px solid var(--bm-border);
                border-radius: 12px; padding: 2rem; min-height: 160px; flex: 1; min-width: 220px;
                display: flex; align-items: center; justify-content: center;
            }
        </style>

        <div class="demo-section">
            <h2>Focus</h2>
            <p><strong>WhileFocus</strong> plays while an element or its descendants are focused.</p>
            <div class="demo-row">
                <div class="demo-card">
                    <Bmotion Animate="Bm.To(scale: 1)"
                            WhileFocus='Bm.To(scale: 1.03, borderColor: "#6c47ff")'
                            Transition="Bm.Tween(0.2)">
                        <input style="background:var(--bm-card);border:2px solid var(--bm-border);color:var(--bm-text);border-radius:8px;padding:.5rem 1rem;outline:none;width:240px"
                               placeholder="Click to focus…"
                               type="text" />
                    </Bmotion>
                </div>
            </div>
        </div>
        """;

    private const string _eventsCode = """
        @using Bit.Bmotion

        <style>
            :root { --bm-primary: #1276C6; --bm-primary-text: #fff; --bm-secondary: #FD7F36; --bm-text: #1A1A1A; --bm-muted: #525252; --bm-card: #F5F5F5; --bm-track: #EBEBEB; --bm-border: #E0E0E0; --bm-radius: 2px; --bm-accent-soft: rgba(18,118,198,.12); --bm-accent-line: rgba(18,118,198,.35); }
            .demo-section { margin-bottom: 3rem; }
            .demo-section h2 { font-size: 1.4rem; font-weight: 700; margin-bottom: .5rem; }
            .demo-section p { color: var(--bm-muted); margin-bottom: 1.5rem; line-height: 1.6; }
            .demo-row { display: flex; flex-wrap: wrap; gap: 1.5rem; align-items: center; }
            .demo-card {
                background: var(--bm-card); border: 1px solid var(--bm-border);
                border-radius: 12px; padding: 2rem; min-height: 160px; flex: 1; min-width: 220px;
                display: flex; align-items: center; justify-content: center;
            }
            .box {
                width: 80px; height: 80px; border-radius: 10px;
                background: linear-gradient(135deg, var(--bm-primary), var(--bm-secondary));
            }
        </style>

        <div class="demo-section">
            <h2>Events</h2>
            <p>Listen to gesture callbacks: <c>OnHoverStart</c>, <c>OnTap</c>, <c>OnTapCancel</c>, etc.</p>
            <div class="demo-row">
                <div class="demo-card" style="flex-direction:column;gap:1rem;">
                    <Bmotion WhileHover="Bm.To(scale: 1.1)"
                            WhileTap="Bm.To(scale: 0.9)"
                            OnHoverStart="@(() => AddEvent("HoverStart"))"
                            OnHoverEnd="@(() => AddEvent("HoverEnd"))"
                            OnTapStart="@(() => AddEvent("TapStart"))"
                            OnTap="@(() => AddEvent("Tap"))"
                            OnTapCancel="@(() => AddEvent("TapCancel"))">
                        <div class="box" />
                    </Bmotion>
                    <div style="font-size:.8rem;color:var(--bm-muted);max-height:120px;overflow:auto;">
                        @foreach (var e in _events.TakeLast(8).Reverse())
                        {
                            <div>@e</div>
                        }
                    </div>
                </div>
            </div>
        </div>

        @code {
            private readonly List<string> _events = new();

            void AddEvent(string name)
            {
                _events.Add($"[{DateTime.Now:HH:mm:ss.ff}] {name}");
                // Only the last 8 entries are ever shown; trim the rest so a long session can't grow this list unbounded.
                if (_events.Count > 8)
                    _events.RemoveRange(0, _events.Count - 8);
                StateHasChanged();
            }
        }
        """;
}
