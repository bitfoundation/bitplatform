// Web/wwwroot/appsettings.json is being shared with blazor server, hybrid and web assembly.

{
    "DetailedErrors": true,
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft": "Warning",
            "Microsoft.Hosting.Lifetime": "Information"
        }
    },
    "AllowedHosts": "*",
    "ApiServerAddress": "http://localhost:5000/api/"
}
