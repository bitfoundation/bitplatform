{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning",
            "Aspire.Hosting.Dcp": "Warning"
        }
    },
    "Parameters": {
        //#if (database == "SqlServer")
        "sqlserver-password": "P@ssw0rd",
        "sqlserver__Comment": "The username is `sa` by default",
        //#elif (database == "PostgreSQL")
        "postgresserver-password": "P@ssw0rd",
        "postgresserver__Comment": "The username is `postgres` by default",
        //#elif (database == "MySql")
        "mysqlserver-password": "P@ssw0rd",
        "mysqlserver__Comment": "The username is `root` by default",
        //#endif
        //#if (filesStorage == "S3")
        "minio-rootPassword": "P@ssw0rd",
        "minio__Comment": "The username is `minioadmin` by default",
        //#endif
        "keycloak-password": "P@ssw0rd",
        "keycloak__Comment": "The username is `admin` by default"
    },
    "Parameters__Comment": "You might need to delete the docker volumes to apply changes to the passwords"
}
