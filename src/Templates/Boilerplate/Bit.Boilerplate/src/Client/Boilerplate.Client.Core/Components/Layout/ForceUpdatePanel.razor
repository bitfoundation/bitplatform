@inherits AppComponentBase

<BitSnackBar @ref=bitSnackBar AutoDismiss="false" DismissIconName="" OnDismiss="OnDismiss">
    <TitleTemplate>
        <BitText Typography="BitTypography.H5">@Localizer[nameof(AppStrings.ForceUpdateTitle)]</BitText>
    </TitleTemplate>
    <BodyTemplate>
        <BitStack>
            <BitText Typography="BitTypography.Body1">@Localizer[nameof(AppStrings.ForceUpdateBody)]</BitText>
            <BitButton>@Localizer[nameof(AppStrings.Update)]</BitButton>
        </BitStack>
    </BodyTemplate>
</BitSnackBar>