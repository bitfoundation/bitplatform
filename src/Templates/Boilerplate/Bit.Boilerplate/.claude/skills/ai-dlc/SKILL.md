---
name: ai-dlc
description: Drives a feature end-to-end using the AI-Driven Development Lifecycle - requirements elaboration, planning and task decomposition, design, implementation, then validation - with explicit user approval before any code is written. Use when the user asks to build a feature properly/end-to-end, wants a structured dev cycle, asks for requirements-then-plan-then-implement, or says "run ai-dlc".
---

<!-- Bridge stub. Claude Code only discovers skills under .claude/skills/, so this file mirrors
     the frontmatter of the canonical skill and delegates to it. Edit the canonical file, not
     this one - only the frontmatter above needs to stay in sync. Tools that read both folders
     see the same name here and land on the same instructions either way. -->

Read `.agents/skills/ai-dlc/SKILL.md` (relative to the repository root) and follow it exactly.
