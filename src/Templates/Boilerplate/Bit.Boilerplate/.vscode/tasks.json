{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "before-build",
            "type": "shell",
            "command": "dotnet build Boilerplate.slnx -t:BeforeBuildTasks",
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}
