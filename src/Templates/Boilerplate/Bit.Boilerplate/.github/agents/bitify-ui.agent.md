---
name: bitify-ui
description: Modernizes Blazor pages by replacing raw HTML elements and custom CSS with Bit.BlazorUI components and theme-aware styling, using the bit BlazorUI MCP tools to discover components and verify their exact APIs.
---

<!-- Bridge stub. Visual Studio's agent picker (and the @ menu) only lists .github/agents/*.agent.md
     files - it discovers the Agent Skills in .agents/skills/ but never lists them for manual
     invocation. This file mirrors the canonical skill's name/description and delegates to it.
     Edit the canonical file, never this one. -->

Read `.agents/skills/bitify-ui/SKILL.md` (relative to the repository root) and follow it exactly.
