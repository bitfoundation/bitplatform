# Command shim for Gemini CLI / Gemini Code Assist agent mode: guarantees /scaffold-entity works
# even where Agent Skills discovery of .agents/skills/ is unavailable or not yet enabled.
description = "Scaffold a complete CRUD entity across every layer (entity, EF config + migration, DTO, Mapperly mapper, controller, IAppController, resx, Blazor pages, navigation)."

prompt = """
Follow these instructions exactly:

@{.github/agents/scaffold-entity.agent.md}

Entity to scaffold: {{args}}
"""
